generator client{
    provider = "prisma-client-js"
}

datasource db{
    provider = "postresql"
}

model User {
id Int
name String 
classid Int
class Class @relation(fields: [classId], references: [id])
}

model Class {
id Int 
name String 
users User[]
}